// downloader.ts — Downloads a YouTube video using yt-dlp.
// Stub implementation — real logic added in Phase 4.

export async function downloadVideo(url: string, outDir: string): Promise<string> {
  // Will use yt-dlp via child_process.spawn in Phase 4
  throw new Error("downloadVideo not yet implemented (Phase 4)");
}
